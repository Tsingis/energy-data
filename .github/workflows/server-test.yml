name: server-test
on:
  pull_request:
    branches:
      - main
    paths:
      - "server/src/**"
      - "server/tests/**"
      - "server/pyproject.toml"
      - "server/uv.lock"
      - "server/requirements.txt"
      - ".github/workflows/server-test.yml"
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.14"
          architecture: "x64"
      - name: Install uv
        run: pip install -r requirements.txt
        working-directory: ./server
      - name: Install dependencies
        run: uv sync --no-build --frozen
        working-directory: ./server
      - name: Check linting & formatting
        run: |
          uv run --no-build ruff check .
          uv run --no-build ruff format .
        working-directory: ./server
  unit-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.14"
          architecture: "x64"
      - name: Install uv
        run: pip install -r requirements.txt
        working-directory: ./server
      - name: Install dependencies
        run: uv sync --no-build --frozen
        working-directory: ./server
      - name: Run unit tests
        run: uv run --no-build python -m unittest discover -v -s tests/unit
        working-directory: ./server
  api-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.14"
          architecture: "x64"
      - name: Install uv
        run: pip install -r requirements.txt
        working-directory: ./server
      - name: Install dependencies
        run: uv sync --no-build --frozen
        working-directory: ./server
      - name: Run API tests
        run: uv run --no-build python -m unittest discover -v -s tests/api
        env:
          FINGRID_API_KEY: ${{ secrets.FINGRID_API_KEY }}
        working-directory: ./server
